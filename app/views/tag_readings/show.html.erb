<main class="pt-3 mt-3">
  <div class="container row margin-auto pt-3">
    <p> HI <%= current_user.first_name.upcase %> </p>
      <h2> Somebody may have found <%= @pet.name.downcase.capitalize %>! </h2>
      <p class="opacity-75"><small> <%= @tag_reading.created_at.strftime("%m/%d/%Y at %I:%M%p") %> </small></p>

      <div class="form--message__wrapper margin-auto my-3" data-controller="speech-bubble">
        <textarea disabled class="form--message" data-speech-bubble-target="message_area"><%= @tag_reading.message %> </textarea>
      </div>


      <div class="mt-3" style="width: 100%; height: 30vh;"
                data-controller="mapbox"
                data-mapbox-markers-value="<%= @markers.to_json %>"
                data-mapbox-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>
    </div>
</main>
